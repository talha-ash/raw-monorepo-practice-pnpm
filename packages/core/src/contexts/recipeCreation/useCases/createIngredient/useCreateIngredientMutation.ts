
export const useLoginMutation = () => {
    // const mutation = useMutation({
    //     mutationFn: async (payload: IngredientSchemaType) => {
    //         const result = await createIn(payload.email, payload.password);
    //         if (result.isErr()) {
    //             return Promise.reject(result.error);
    //         }
    //         return result.value.data;
    //     },
    // });
    // return { mutation };
};
